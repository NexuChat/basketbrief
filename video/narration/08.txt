One Strands agent on Amazon Nova Pro. The model decides what a source means, what is missing, and who to ask. The code decides what is allowed: an amount must appear in its source, baskets never become households, and a known gap is re-checked after the model's turn, because the model does not always remember to ask.
