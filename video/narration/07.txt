Both reports are redrafted. The arithmetic no longer closes, so BasketBrief says exactly that instead of quietly picking a number. And the approval bound to the delivered version is refused. It cannot be reused for a version nobody read.
